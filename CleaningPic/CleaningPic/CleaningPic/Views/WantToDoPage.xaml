<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CleaningPic.Utils"
             x:Class="CleaningPic.Views.WantToDoPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:ImageConverter x:Key="ImageConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell Text="{Binding Dirt}" Detail="{Binding Method}" ImageSource="{Binding ImageData, Converter={StaticResource ImageConverter}}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button
                Text="Add list item button"
                Clicked="AddListItemButton_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>